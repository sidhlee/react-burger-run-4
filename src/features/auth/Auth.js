import React, { Component } from "react";
import styled from "styled-components";

const StyledAuth = styled.div``;

class Auth extends Component {
  render() {
    return <StyledAuth></StyledAuth>;
  }
}

export default Auth;
